<UserControl x:Class="T1S.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:T1S.Views"
             xmlns:vm="clr-namespace:T1S.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:MainViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Anti-Debug">
                <local:AntiDebugView/>
            </TabItem>
            <TabItem Header="Entropy">
                <local:EntropyView/>
            </TabItem>
            <TabItem Header="Logs">
                <local:LogsView/>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="1" Background="{StaticResource Button.Static.Background}">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Assembly path:" Margin="20,0,0,0" VerticalAlignment="Bottom"/>

            <Grid Grid.Row="1" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding BinaryPath}" Height="25" Margin="20,0,10,0"/>
                <Button Grid.Column="1" Content="Scan" Command="{Binding ScanCommand}" Height="25" Margin="10,0,20,0"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
